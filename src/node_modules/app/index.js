const express = require('express');

module.exports = (service) => {
  const router = express.Router();

  router.use('/prizes');
  router.use('/tickets');
  router.use('/users');

  return router;
};
